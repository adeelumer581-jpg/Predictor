services:
  - type: web
    name: stock-predictor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python comprehensive_system.py
    envVars:
      - key: PORT
        value: "5003"
        sync: false